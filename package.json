{
  "name": "@burneikis/pi-nolo",
  "version": "1.1.0",
  "description": "No-YOLO mode for pi — gates write, edit, and bash tool calls behind user confirmation with configurable safe-command allowlists and YOLO override modes.",
  "keywords": [
    "pi-package"
  ],
  "license": "MIT",
  "author": "burneikis",
  "repository": {
    "type": "git",
    "url": "https://github.com/burneikis/pi-nolo.git"
  },
  "scripts": {
    "test": "tsx --test tests/*.test.ts"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*"
  },
  "pi": {
    "extensions": [
      "./index.ts"
    ]
  },
  "devDependencies": {
    "@types/diff": "^7.0.2",
    "tsx": "^4.21.0"
  },
  "dependencies": {
    "diff": "^8.0.4"
  }
}
